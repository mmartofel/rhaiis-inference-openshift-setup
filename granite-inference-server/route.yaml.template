apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: granite
  namespace: ${NAMESPACE}
spec:
  to:
    kind: Service
    name: granite
  port:
    targetPort: 8000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None